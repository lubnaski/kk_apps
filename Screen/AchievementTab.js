import React, {Component} from 'react';
import {Platform, ScrollView, Navigator, Slider, StyleSheet, Text, View, Image, TouchableOpacity, Picker, AsyncStorage } from 'react-native';
import { createBottomTabNavigator } from 'react-navigation';
import { Card, Left, CardItem, Title, Container, Right, Content, Header, Body, Icon } from 'native-base';



export default class AchievementTab extends React.Component{
  render(){
    return(
      <Text>Lalalala</Text>
      );
  }
}
